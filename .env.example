# Supabase public config — copy this file to `.env.local` and fill in.
# Both values are safe to expose in the client bundle; data is protected by
# Row Level Security on Supabase. Find them in:
#   Supabase Dashboard → Project Settings → API
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
